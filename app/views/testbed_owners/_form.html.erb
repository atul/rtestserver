<%= form_for(@testbed_owner) do |f| %>
  <% if @testbed_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testbed_owner.errors.count, "error") %> prohibited this testbed_owner from being saved:</h2>

      <ul>
      <% @testbed_owner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :coreid %><br />
    <%= f.text_field :coreid %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
